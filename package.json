{
  "private": true,
  "scripts": {
    "prepare": "lerna bootstrap",
    "release": "lerna publish from-package",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
